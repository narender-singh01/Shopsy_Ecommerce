const websiteUrl = process.env.REACT_APP_SITE_URL;
const serverAssetsUrl = process.env.REACT_APP_ASSETS_URL;
const config = {
  apiEndPoints: {
    productList: "/products",
  },
};

module.exports = config;
